import HeaderTransparentGhost from "./HeaderTransparentGhost";

// This build is locked to the transparent-ghost header variant.
export default function Header() {
  return <HeaderTransparentGhost />;
}
